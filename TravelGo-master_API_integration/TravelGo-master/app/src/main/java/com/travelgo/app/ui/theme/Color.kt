package com.travelgo.app.ui.theme

import androidx.compose.ui.graphics.Color

// 🌞 Paleta de colores claros (modo claro)
val VerdePrimario = Color(0xFF4ADE80)       // Verde principal
val VerdeSecundario = Color(0xFF2DD4BF)     // Verde agua / acento secundario
val FondoSuave = Color(0xFFF6F7F8)          // Fondo general claro
val CardColor = Color(0xFFFFFFFF)           // Tarjetas o superficies blancas
val TextoPrincipal = Color(0xFF000000)      // Texto negro
val TextoBoton = Color(0xFFFFFFFF)          // Texto blanco dentro de botones

// 🌙 Paleta de colores oscuros (modo oscuro)
val DarkBg = Color(0xFF0F1115)              // Fondo oscuro
val DarkCard = Color(0xFF1A1D23)            // Superficie elevada oscura
val DarkTextPrimary = Color(0xFFE5E7EB)     // Texto claro

// 🎯 Alias (compatibilidad con temas antiguos)
val Accent = VerdePrimario
val AccentSecondary = VerdeSecundario
